type User {
  username: String!
  email: String!
  password: String!
  createdAt: Date
  token: String
  avatar: String
  dailyMood: String
}

type Token {
  token: String!
}

type Query {
  me: User!
}

type Mutation {
  signupUser(
    username: String!
    email: String!
    password: String!
    avatar: String
  ): Token!
  signinUser(email: String!, password: String!): Token!
  updateDailyMood(mood: String!): User
}
